# MEGPNM runtime dependencies.
#
# NOTE ON DGL: DGL is not installable from PyPI for CUDA builds. Install it
# first from the official wheel index, matching your CUDA version, e.g.
#
#   pip install torch==2.4.0 --index-url https://download.pytorch.org/whl/cu121
#   pip install dgl==2.4.0 -f https://data.dgl.ai/wheels/torch-2.4/cu121/repo.html
#
# then `pip install -r requirements.txt` for the rest.
#
# Versions below are the ones used for the results reported in the paper
# (Python 3.11.13, CUDA 12.1).

torch==2.4.0
dgl==2.4.0
rdkit==2025.09.1
numpy==2.2.6
pandas==2.3.3
scikit-learn==1.7.2
scipy==1.16.0
tqdm==4.67.1
